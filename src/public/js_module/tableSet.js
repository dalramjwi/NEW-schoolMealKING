// import { buttonSet } from "./buttonSet.js";
const tableSet = function (row, col, path) {
  for (let i = 0; i < row * col; i++) {
    console.log("반복");
  }
  // const div = document.createElement("div");
};

const test = tableSet(3, 3);
